Python Commands

cd /home/kang/Scripts/environments
python3 -m venv my_env
ls my_env
source my_env/bin/activate
sudo vim hello.py
print("Hello, World!")
deactivate
sudo pip3 install wheel --no-binary :all:

pyproject.toml, run python -m pip install
poetry install
poetry run pytest tests/
poetry run pre-commit install
poetry run pre-commit run --all-files
